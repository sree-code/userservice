# Railway Configuration - FORCE NO DOCKER
# Explicitly disable Docker and force Nixpacks

[build]
builder = "NIXPACKS"

[deploy]
healthcheckPath = "/api/user/health"
restartPolicyType = "ON_FAILURE"

# Explicitly disable Docker
[variables]
NIXPACKS_DOCKER = "false"
