# Railway automatically detects Spring Boot projects
# This file is optional but can specify custom settings

[build]
command = "mvn clean package -DskipTests"

[deploy]
start_command = "java -jar target/userService-0.0.1-SNAPSHOT.jar"
health_check_path = "/health"
port = 8080

[env]
SPRING_PROFILES_ACTIVE = "prod"
PORT = "8080"
